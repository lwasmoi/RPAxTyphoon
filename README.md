# RPAxTyphoon - Nong Thun AI (น้องทุน) 🤖

น้องทุน (Nong Thun) คือ AI Assistant สำหรับช่วยเหลือผู้ใช้งานในระบบเบิกจ่ายงบประมาณและทุนวิจัย (RPA) ของมหาวิทยาลัยสงขลานครินทร์ (PSU) ถูกพัฒนาด้วยสถาปัตยกรรม Advanced RAG (Retrieval-Augmented Generation) เพื่อให้คำตอบที่แม่นยำ อ้างอิงได้ และปลอดภัยต่อระเบียบของมหาวิทยาลัย

## ✨ Key Features (จุดเด่นของระบบ)

- **Advanced RAG Pipeline:** มีระบบปรับแก้คำถาม (Query Rewriter) และคัดกรองเนื้อหา (Custom Domain Reranking) เพื่อดึงคู่มือและสถานะทุนวิจัยที่ถูกต้องที่สุด
- **Background Auto-Sync:** อัปเดตฐานความรู้จาก PostgreSQL แบบอัตโนมัติ (Zero-downtime) โดยไม่รบกวนการใช้งานของ User
- **Strict Anti-Hallucination:** ควบคุม Prompt เพื่อป้องกัน AI คิดคำนวณวัน-เวลาเอง และป้องกันการตอบกลับเป็นภาษาอื่นที่ไม่ใช่ภาษาไทย
- **Session & Feedback Log:** บันทึกประวัติการสนทนาและการประเมิน (Thumbs Up/Down) ลงฐานข้อมูลเพื่อการพัฒนาต่อยอด

## 🛠️ Tech Stack

- **Frontend:** Streamlit
- **AI & RAG:** OpenAI SDK (รองรับ Typhoon/Ollama), NumPy (Vector Cache)
- **Database:** PostgreSQL (psycopg2)
- **Background Job:** APScheduler
- **Deployment:** Docker, Nginx

## 🚀 How to Run (วิธีจำลองการทำงานด้วย Docker)

1. **ตั้งค่า Environment:**
   สร้างไฟล์ `.env` ใน Root Directory และกำหนดค่าต่อไปนี้ (ดูรูปแบบจาก `config.py`):
   ```env
   ACTIVE_MODE=CLOUD
   DB_HOST=your_db_host
   DB_PORT=your_db_port
   DB_NAME=your_db_name
   DB_USER=your_db_user
   DB_PASS=your_db_password
   # ... และ API Keys อื่นๆ
